# 🔐 الاتصال بين Git و GitHub باستخدام SSH 

هذا الدليل خطوة بخطوة يساعدك على توصيل Git المحلي الخاص بك بـ GitHub بطريقة آمنة باستخدام مصادقة SSH.

---

## ✅ الخطوة 1 — إنشاء مفاتيح SSH على جهازك

افتح الطرفية (Git Bash على ويندوز) واكتب:

```bash
ssh-keygen -t ed25519 -C "your_email@example.com"
```
اضغط Enter لحفظ المفتاح في المسار الافتراضي.

بعد الانتهاء، سيكون لديك:

المفتاح الخاص (Private Key) → يبقى على جهازك ولا تشاركه أبداً

المفتاح العام (Public Key) → ستقوم بإضافته إلى GitHub

انسخ المفتاح العام:

```bash
cat ~/.ssh/id_ed25519.pub
```
✅ الخطوة 2 — إضافة مفتاح SSH إلى حساب GitHub
انتقل إلى GitHub

اضغط على صورة الملف الشخصي → Settings

اختر SSH and GPG keys

اضغط New SSH Key

الصق المفتاح العام

اضغط Add SSH key

الآن يستطيع GitHub التعرف على جهازك.

✅ الخطوة 3 — استخدام SSH في المستودع (Repository)
بعد إنشاء مستودع جديد على GitHub:

افتح صفحة المستودع

اضغط على زر Code

اختر SSH

انسخ رابط SSH

يجب أن يكون على هذا الشكل:

```bash
git@github.com:username/repository.git
```
الآن يمكنك استنساخ المستودع:

```bash
git clone git@github.com:username/repository.git
```
إذا كان لديك المستودع محليًا بالفعل، اربطه بالمستودع الجديد:

```bash
git remote set-url origin git@github.com:username/repository.git
```
✅ لخطوة 4 — اختبار الاتصال
```bash
ssh -T git@github.com
```
إذا نجح، سترى:

`Hi username! You've successfully authenticated.`

🎉 تم! تم توصيل Git بـ GitHub باستخدام SSH بنجاح.
